# Same physical DB used by the crawler in local development.
OSINT_DB_PATH=../DarkWeb-Deanonymization/data/crawler.db

# Checked-out osint-engine repository.
OSINT_ENGINE_PATH=../osint-engine

# Checked-out shared schema directory.
SHARED_SCHEMA_PATH=../shared/schema_sqlite.sql

# Configure only after confirming the crawler CLI.
# The process receives TARGET and JOB_ID environment variables.
CRAWLER_COMMAND=
